---
title: "数据可视化"
date: 2021-03-07
draft: true
tags:
- 学 
- python 
- sns
---

#### 1章:

```python
#读取特定列的语法:
pd.read(,usecols = [''])
#分类变量:
df.describe(include = object)
#编码:
np.where(df[]>3000,1,0)
#取整,进一:
import math
s.appley(math.ceil)
#判断取值
bar = 'yes' if .. else 'no'
#apply应用行
df.apply(func,axis=1)
#drop删除返回副本
df.drop(columns = [''],inplace=True)
#写入文件index处理
df.to_csv('',index=False)
#绘图
import seaborn as sns
sns.distplot(df.s,kde=False,bins=100)
#对数
sns.distplot(np.log(df.s))
#每个分类的统计
pd.crosstab(index=df.s,columns='count')
#分类统计柱状图,aspect高度相关的
sns.catplot('cut',data=df,aspect=1.5,kind='count',color='b')
#平均,置信区间,x轴的排序顺序
from numpy import mean
sns.set(style='whitegrid')
ax = sns.barplot(x='',y='',data=df,estimator=mean,ci = 68,order=[])
#嵌套分组
ax = sns.barplot(x='',y='',hue='',data=df)
ax.legend(loc='upper right',ncol=4)#图例位置
ax.set_xlabel('',fontdict={'fontsize':15})
ax.set_xticklabels(ax.get_xticklabels(),fontsize=13,rotation=30)#轴项处理
#标注
x = df.index.tolist()[0]
y = len(df)
plt.annotate('',xy=(x,y),xytext=(x+,y+),arrowprops = dict(facecolor='red'))#箭头颜色
```

#### 2章:

```python
#散点图
ax = sns.scatterplot(x=,y=,data=df)
#静态六边形图
sns.set(style=ticks)
sns.jointplot(df.s,df.s.kind='hex',color='#4CB391')
#等高线图
sns.set_style('white')
sns.kdeplot(df.s,df.s,shade=True)
#静态折线图
sns.lineplot(x='',y='',data=df,ci=68)
#静态热图
df.pivot('x','y','我们感兴趣的变量')
ax = sns.heatmap(df)
#行和列聚类
ax = sns.clustermap(df,col_cluster=False,row_cluster=True)#行聚类
#距离的选项
ax = sns.clustermap(df,row_cluster=False,metric='correlation'/'euclidean'/'manhattan')
#热图中锁的概念
#不同聚类中最接近的两个点单连锁
#相聚最远的两个点,全连锁
#两个聚类中所有可能行对的平均距离,平均连锁
ax = sns.clustermap(df,col_cluster=False,metric='correlation',method = 'average')
ax = sns.clustermap(df,col_cluster=False,metric='correlation',method = 'complete')
ax = sns.clustermap(df,col_cluster=False,metric='correlation',method = 'single')
#直方图的线
import matplotlib as plt
ax =sns.distplot(df.s,bins=50,kde=False)
plt.axvline(x=np.mean(df.s),color='red',label='mean')
plt.axvline(x=np.median(df.s),color='orange',label='median')
plt.legend(loc='uper right')
#箱形图
sns.boxplot(x='',y='',data=df)
#分组箱图
sns.boxplot(x='',y='',data=df,hue='')
#静态小提琴图
sns.viloinplot(x=''y='',data,hue='')
#学会看分组箱图和小提琴图含义

```

